---
title: Automatic cache prewarming for compute updates, new guides for AI agents and branching
---

## Automatic cache prewarming for compute updates

To apply [updates](/docs/manage/updates) to your Neon compute (Postgres upgrades, security patches, and the like), we restart the compute where Postgres runs during your [update window](/docs/manage/updates#updates-on-paid-plans). The restart itself typically takes only a few seconds, but in-memory caches are left cold, which can impact query performance until they warm up again.

To protect performance, we now prewarm your compute's cache during the update process without affecting restart times. There are no additional compute or storage costs associated with this enhancement. In the coming weeks, cache prewarming will roll out to all read/write computes, with read replica computes to follow.

For the technical details, see our blog post: [Improving database upgrades with prewarming](BLOG_POST_URL_PLACEHOLDER).

## AI agents and database branching

**Neon branching** and **AI coding agents** work well together. Branching gives each run an isolated Postgres copy in seconds, so agents can migrate schema, run tests, and try risky changes safely. The guides below show this workflow with Codex and Claude Code.

- [**Safe AI-powered schema refactoring with OpenAI Codex and Neon**](/guides/openai-codex-neon-mcp): Use **OpenAI Codex CLI** with the **Neon MCP Server** so Codex can create an isolated branch, run Drizzle migrations, and validate schema refactors safely.

- [**Isolated Subagents: Running Claude Code in parallel with Neon Database Branching**](/guides/isolated-subagents-neon-branching): Use a `post-checkout` hook to give each **Claude Code subagent** its own Git worktree and **Neon branch**, so parallel agents can run without code or database collisions.

<details>
<summary>**Fixes & improvements**</summary>

- For the [project consumption metrics endpoint](https://api-docs.neon.tech/reference/getconsumptionhistoryperprojectv2), the `metrics` parameter is now explicitly required, so API requests must include at least one metric.

</details>

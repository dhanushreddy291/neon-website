---
title: Neon MCP scoping, one-command setup supports more agents, improved billing support, and more
---

## Neon MCP Server scope categories

The hosted [Neon MCP Server](/docs/ai/neon-mcp-server) now supports **[scope categories](/docs/ai/neon-mcp-server#scope-categories)**: use the **`X-Neon-Scopes`** header with an **API key**, or choose categories when you **authorize with OAuth**.

Supported categories include **`projects`**, **`branches`**, **`schema`**, **`querying`**, **`neon_auth`**, **`data_api`**, and **`docs`**. See [Scope categories](/docs/ai/neon-mcp-server#scope-categories) in the Neon MCP Server docs for what each category covers.

**When authenticating with an API key**, pass a comma-separated list of category names (lowercase). For example, to allow **querying** tools only:

```json
{
  "mcpServers": {
    "neon": {
      "type": "http",
      "url": "https://mcp.neon.tech/mcp",
      "headers": {
        "Authorization": "Bearer <NEON_API_KEY>",
        "X-Neon-Scopes": "querying"
      }
    }
  }
}
```

Additionally, the MCP server exposes **only tools your connection is allowed to use**, so assistants are less likely to suggest actions that would fail at runtime.

Combine category scope with **read-only** mode and **single-project** locking for additional control. See [Scoping and access control (remote MCP)](/docs/ai/neon-mcp-server#scoping-and-access-control-remote-mcp).

## One-command setup for more AI assistants

The **`npx neonctl@latest init`** command, which sets up Neon and configures the Neon MCP Server for you, now supports more AI assistants including **VS Code**, **Claude Code**, **Cursor**, **Claude Desktop**, **Codex**, **OpenCode**, **Antigravity**, **Cline**, **Cline CLI**, **Gemini CLI**, **GitHub Copilot CLI**, **Goose**, **MCPorter**, and **Zed**.

Run it from the root directory of your project:

```bash
npx neonctl@latest init
```

The **`init`** command uses **add-mcp** to configure the [Neon MCP Server](/docs/ai/neon-mcp-server) for your AI assistant. See [Supported agents (add-mcp)](/docs/ai/connect-mcp-clients-to-neon#supported-agents-add-mcp) for the full list of supported agents.

## Request billing support from the Billing page

![Request billing support button](/docs/changelog/request_billing_support.png)

Paid plans can open **Request billing support** on the **Billing** page. The form lets you choose an invoice and describe your issue.

## Tables page enhancements

The Drizzle Studio integration that powers the [**Tables**](/docs/guides/tables) page in the Neon Console has been updated to version **1.4.0**. This release includes reworked pagination, column reordering, dedicated sorting controls, and better copy and paste for selected cells, among other updates. For the full list, see the [Neon Drizzle Studio Integration Changelog](https://github.com/neondatabase/neon-drizzle-studio-changelog/blob/main/CHANGELOG.md).

## New examples and guides

- **[neon-auth-orgs-example](https://github.com/neondatabase/neon-js/tree/main/examples/neon-auth-orgs-example)** in [neondatabase/neon-js](https://github.com/neondatabase/neon-js): A small multi-tenant todo app that demonstrates **Neon Auth’s Organization plugin** end to end. [Learn more about the Organization plugin](/docs/auth/guides/plugins/organization). For other Neon Auth samples, browse [`examples/`](https://github.com/neondatabase/neon-js/tree/main/examples) in that repo.

- [**Build your own Full-Stack Cloud Agents with Cloudflare Sandboxes and Neon Database Branching**](/guides/cloudflare-sandbox-neon-branching): Run a Cloudflare Sandbox worker that creates a **Neon branch per task**, runs an agent such as Claude Code with a branch-specific **`DATABASE_URL`**, then commits and opens a pull request.

- [**Triaging pull requests with OpenCode and Neon Database Branching**](/guides/opencode-neon-github-actions): Use **GitHub Actions** with OpenCode and Neon's **`create-branch-action`**, so each issue-triggered run gets its own Postgres branch for schema changes, migrations, and validation before the PR.
